pipeline {
    agent {
        lebel 'server1'
    }
    stages {
        stage('Build') {
            steps {
                echo 'Hello'
            }
        }
    }
    post {
        success {
            echo 'Completed'
        }
    }
}
